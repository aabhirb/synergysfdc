({
	doInit : function(component, event, helper) {
        helper.putCheckboxWrapper(component, event, helper);
	},
    
    handleShowModal : function(component, event, helper){
        helper.showModal(component, event, helper);
    }

})