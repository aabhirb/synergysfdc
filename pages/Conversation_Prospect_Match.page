<apex:page showHeader="false" standardController="Prospect_Match__c">
	
	<apex:includeLightning rendered="true"/>
	<div id="lightning" />

	<script>
		var recordId = "{!$CurrentPage.parameters.id}";
		$Lightning.use(
			"smagicinteract:ConversationsApp", 
			function() {
				$Lightning.createComponent(
					"smagicinteract:conversations",
					{
						 recordId : recordId,
						 sObjectName : "Prospect_Match__c",
						 phoneFieldCSV : "",
						 embedContext : "detail"
					},
					"lightning",
					function(cmp) {
					}
				);
			}
		);
	</script>
</apex:page>